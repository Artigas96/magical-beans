{
  "name": "magical-beans",
  "version": "1.0.14",
  "scripts": {
    "build": "gulp build",
    "build:watch": "gulp watch",
    "link-project": "gulp link",
    "clean": "gulp clean",
    "clean:link": "gulp link --clean",
    "changelog": "standard-version --release-as ${RELEASE_VERSION}",
    "apply-version": "node ./scripts/update-module-version.js",
    "tag-release": "np --no-tests --no-publish --no-release-draft"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/stream": "^3.0.1",
    "fs-extra": "^11.3.2",
    "gulp": "^5.0.1",
    "gulp-autoprefixer": "^10.0.0",
    "gulp-dart-sass": "^1.1.0",
    "gulp-sourcemaps": "^3.0.0",
    "np": "^10.2.0",
    "standard-version": "^9.5.0",
    "vinyl-buffer": "^1.0.1",
    "vinyl-source-stream": "^2.0.0"
  },
  "dependencies": {
    "nanoid": "^5.1.6"
  }
}
